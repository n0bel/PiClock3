kind: forecast-source

# a daily forecast does not change quickly, and the free tier is meant to
# be treated gently
refresh: 30

# the classic layout has room for 9
forecast-days: 9

location:
    latitude: "{location.latitude}"
    longitude: "{location.longitude}"
    timezone: "{location.timezone}"
    elevation: "{location.elevation}"
