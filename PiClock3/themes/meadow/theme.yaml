name: Meadow
description: Butterflies over a bright meadow, dark-blue clock

background: 'background.png'

# art sets the plugins ship; a theme picks which
clock-art:     darkblue
weather-icons: icons-darkblue

default:
  font-size: 0.02        # a fraction of the screen height
  font-family: Arial
  background-color: transparent
  # this background is bright, so the clock is dark:
  # #bef is for the dark ones and gives 1.7:1 here, which is unreadable
  color: '#042299'

borders:
  # a 3x3 sheet, so the slice is a third of it - the art describes its own
  # geometry and this file cannot disagree with it.  width is a fraction of
  # screen height, so a frame is the same weight on a small box as on a big
  # one, and the same at 800x600 as at 1920x1080.
  default:
    art: 'frame.png'
    width: 0.012
    inset: 0.5
  radar:
    art: 'frame.png'
    width: 0.012
    inset: 0.5

styles:
  centered:
    font-size: 0.6
    qproperty-alignment: AlignCenter
  date:
    font-size: 0.75
    qproperty-alignment: AlignCenter
  bottom:
    # stays narrow enough to pass under the forecast column
    font-size: 0.36
    qproperty-alignment: AlignCenter
