kind: analog-clock
clock-images-folder: lightblue
clock-images-base-folder: '{plugin-folder}'

# the theme picks the art set; lightblue is the fallback

# the zone this face shows.  Blank is the clock's own, so a wall of them
# each name a city while one clock on a wall names none.
timezone: '{location.timezone}'
