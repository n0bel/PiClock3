kind: clock
color: "#50CBEB"
extra-font-attributes: ""
format: "{plugin-data.now:%I:%M\n%S %p}"  # The format of the time

# a clock face reads better lowercased; a date does not
lowercase: true

# a fraction of the height of the region it sits in
font-size: 0.3

from-theme:
  color: default.color
